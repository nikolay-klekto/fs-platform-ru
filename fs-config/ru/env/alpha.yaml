env:
  link:
    domain: ru-alpha.funscrut.by
    db:
      mongo:
        cluster:
          hosts: ["db.alpha.fsru:27017"]
          credentials: ["app@admin:dafUZT2dbOSp0BbcxADF"]
      funscrut:
        outputSchema: ru_master_funscrut
        host: db.alpha.fsru
        port: 3306
        username: root
        password: root
    esia:
      redirect:
        url: https://${env.link.domain}/registration/static/step2-2.html
    eventbus:
      hostname: db.alpha.fsru
      username: guest
      password: guest
      exchangeName: ru-test.event-bus.exchange
