deploy_script_version: '2.2'

environment:
  app: 'ru'
  environment: 'alpha'
  datacenters:
    - dc1
  consul_host: 'infra'
  # extends: base.yaml

services:
  config-pusher:
    mem_limit: 4g
    cpu_limit: 200
    version: null


  ##################################################
  #                    service                     #
  ##################################################

  service-initial-registration:
    mem_limit: 512m
    cpu_limit: 200
    version: 1.212.14-MMRU-SNAPSHOT
