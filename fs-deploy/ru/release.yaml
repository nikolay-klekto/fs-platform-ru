deploy_script_version: '1.0'

environment:
  app: 'ru'
  environment: 'release'
  datacenters:
    - ru-release
  consul_host: '192.168.192.14'

services:

  config-pusher:
    mem_limit: 3g
    cpu_limit: 200
    version: null